# --- Acesso a dados (BigQuery / camada Gold da Fase 2) -----------------------
google-cloud-bigquery>=3.25
db-dtypes>=1.2
pyarrow>=15.0
PyYAML>=6.0

# --- Núcleo científico -------------------------------------------------------
numpy>=1.26
pandas>=2.0
scipy>=1.11          # correlações, distribuição normal no risco de meta

# --- Machine Learning --------------------------------------------------------
scikit-learn>=1.4    # TargetEncoder e StratifiedGroupKFold exigem >= 1.3
lightgbm>=4.3
xgboost>=2.0
optuna>=3.6          # busca de hiperparâmetros sob CV agrupada
joblib>=1.4          # serialização da pipeline treinada

# --- Interpretabilidade ------------------------------------------------------
shap>=0.45

# --- Visualização ------------------------------------------------------------
matplotlib>=3.8      # única biblioteca de gráficos: o estilo do projeto é próprio
                     # (src/visualization/estilo.py), com paleta validada para
                     # daltonismo — por isso não há seaborn aqui

# --- Notebooks ---------------------------------------------------------------
jupyter>=1.0
ipykernel>=6.29
nbformat>=5.9        # `make notebooks` gera os .ipynb programaticamente

# --- Testes ------------------------------------------------------------------
pytest>=8.0
